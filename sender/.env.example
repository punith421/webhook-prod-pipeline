PORT=5000
NODE_ENV=production

# Must match the receiver's WEBHOOK_SECRET exactly.
WEBHOOK_SECRET=

# Default receiver webhook URL, prefilled in the dashboard.
RECEIVER_WEBHOOK_URL=http://localhost:4000/webhook

LOG_LEVEL=info
